<!DOCTYPE html>
<html>
<head>
    <title>Manage players</title>
</head>
<body>
<table style="width:20%">
    <tr>
        {{ form_start(addPlayerForm) }}
        {{ form_widget(addPlayerForm) }}
        {{ form_end(addPlayerForm) }}
    </tr>
    <tr>
        {% for player in allPlayers %}
    <tr>
        <td>
            {{ player.name }}
        </td>
        <td><a href="{{ path('manage-players-remove', {'id': player.id}) }}">click</a></td>
    </tr>
        {% endfor %}
</table>
</body>
</html>